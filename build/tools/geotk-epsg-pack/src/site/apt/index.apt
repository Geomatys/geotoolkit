EPSG pack

  Compact the EPSG data script file. This module is used only when the <<<geotk-epsg>>> module
  needs to be updated for a new release of the EPSG database. The SQL scripts to modify are
  located in the directory below, which is called "<<<geotk-epsg>>> resources directory" for
  the remainder of this page:

-----------------------------------------------------------------------------------------------------------------
<Geotoolkit directory>/modules/referencing/geotk-epsg/src/main/resources/org/geotoolkit/referencing/factory/epsg/
-----------------------------------------------------------------------------------------------------------------

  Steps to follow:

  [[1]] Copy the new EPSG scripts in the <<<geotk-epsg>>> resource directory. Every existing files
        should be overwritten except <<<Indexes.sql>>> (which is left unchanged) and <<<Data.sql>>>,
        which should be copied under a temporary name (this page use <<<Data-orig.sql>>>). Note that
        files are renamed with the format-specific and version-specific parts of the filename removed.
        For example <<<EPSG_v6_18.mdb_Data_MySQL.sql>>> is saved as <<<Data.sql>>>.

  [[2]] Open the <<<Table.sql>>> file for edition. In the statement creating <<<epsg_datum>>> table,
        change the type of <<<realization_epoch>>> column to <<<INTEGER>>>.

  [[3]] Run the following command-lines from the directory containing the scripts, where <<<$GEOTK_HOME>>>
        is the root of the Geotoolkit.org Maven project:

--------------------------------------------------------------------------------------------------------------
cp -i $GEOTK_HOME/build/tools/geotk-epsg-pack/target/geotk-epsg-pack-SNAPSHOT.jar $GEOTK_HOME/target/binaries/
java -jar $GEOTK_HOME/target/binaries/geotk-epsg-pack-SNAPSHOT.jar compact Data-orig.sql Data.sql
rm Data-orig.sql
--------------------------------------------------------------------------------------------------------------

  [[4]] Upgrade the <<<VERSION>>> value defined in the <<<ThreadedEpsgFactory>>> class.

  [[5]] Clean and build (<<<mvn clean install>>>).
