This directory has no "package-info.java" file because
it is defined in the geotk-metadata-sql module. We can
not put the SpatialDatabase class in the above-cited
module because of its dependency to the referencing
module.
